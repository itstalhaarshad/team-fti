"""Agent 1 — Rubric Architect (runs once per batch).

Turns sample answer image and/or free-text guidelines into a structured Rubric and writes
it to shared memory.

TODO(phase1): build_rubric(parts, guidelines) -> Rubric  via core.llm.generate_json.
"""
